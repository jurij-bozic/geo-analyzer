[phases.setup]
nixPkgs = ['nodejs_20', 'pnpm']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = [
  'pnpm --filter=@geo-analyzer/shared run build',
  'pnpm --filter=@geo-analyzer/worker run build'
]

[start]
cmd = 'cd apps/worker && node dist/index.js'
